apiVersion: v1
kind: Service
metadata:
  name: inventory-api
  namespace: default
  labels:
    app: inventory-api
spec:
  type: LoadBalancer
  selector:
    app: inventory-api
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
